{{#if tailwind}}
import { cn } from "{{#if shadcn}}@/lib/utils{{else}}../utils/cn{{/if}}";
{{/if}}
{{#if styledComponents}}
import styled from "styled-components";
{{/if}}
{{#if cssModule}}
import styles from "./{{name}}.module.css";
{{/if}}

export interface {{pascalName}}Props {}

export function {{pascalName}}(props: {{pascalName}}Props) {
  return (
{{#if styledComponents}}
    <Styled{{pascalName}}>{ {{pascalName}} }</Styled{{pascalName}}>
{{else}}
  {{#if cssModule}}
    <div className={styles.container}>{ {{pascalName}} }</div>
  {{else}}
    {{#if tailwind}}
    <div className={cn("")}>{ {{pascalName}} }</div>
    {{else}}
    <div>{ {{pascalName}} }</div>
    {{/if}}
  {{/if}}
{{/if}}
  );
}
