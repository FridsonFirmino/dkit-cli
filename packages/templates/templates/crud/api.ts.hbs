import type { {{pascalName}}, Create{{pascalName}}Input, Update{{pascalName}}Input } from "./types";

const API_BASE = "/api/{{kebabName}}s";

export const {{camelName}}Api = {
  async getAll(): Promise<{{pascalName}}[]> {
    const response = await fetch(API_BASE);
    return response.json();
  },

  async getById(id: string): Promise<{{pascalName}}> {
    const response = await fetch(`${API_BASE}/${id}`);
    return response.json();
  },

  async create(data: Create{{pascalName}}Input): Promise<{{pascalName}}> {
    const response = await fetch(API_BASE, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify(data),
    });
    return response.json();
  },

  async update(id: string, data: Update{{pascalName}}Input): Promise<{{pascalName}}> {
    const response = await fetch(`${API_BASE}/${id}`, {
      method: "PATCH",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify(data),
    });
    return response.json();
  },

  async delete(id: string): Promise<void> {
    await fetch(`${API_BASE}/${id}`, { method: "DELETE" });
  },
};
